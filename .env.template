# Google Cloud Project ID (Required)
VERTEX_PROJECT_ID=formal-flow-3rmrd

# Vertex AI Region (e.g., us-central1, europe-west4)
VERTEX_LOCATION=us-central1

# Gemini Model ID (Recommended: gemini-2.5-flash for batch efficiency)
VERTEX_MODEL_ID=gemini-2.5-flash

# --- GCS Configuration ---

# Bucket for Batch Prediction Inputs/Outputs (JSONL files)
# This bucket is used by the Vertex AI Batch Prediction jobs.
# Default: ecfr-dereg-dash-batch
GCS_BUCKET=ecfr-dereg-dash-batch

# Bucket for storing raw XML files downloaded from GovInfo
# This is where the initial data ingestion lands.
# Default: ecfr-raw-xml
RAW_XML_BUCKET_NAME=ecfr-raw-xml

# Prefix (folder) within the raw XML bucket
# Default: raw
RAW_XML_PREFIX=raw

# Bucket for storing processed Parquet files
# This is the "Data Lake" storage for analysis.
# Default: ecfr-parquet
PARQUET_BUCKET_NAME=ecfr-parquet

# Prefix (folder) within the Parquet bucket
# Default: parquet
PARQUET_PREFIX=parquet

# --- Local Development ---

# Local data directory for temporary files and SQLite DB
# Default: ./data
DATA_DIR=./data

# Enable DuckDB Web UI (1 = enabled, 0 = disabled)
DUCKDB_UI=1